{
  "buildCommand": "cd packages/prisma && npm install --no-save && npx prisma generate && cd ../../apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs"
}
